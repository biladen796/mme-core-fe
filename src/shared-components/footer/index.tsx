'use client';
import React from 'react';

const Footer = () => {
  return (
    // <ul>
    //   {items.map((item) => (
    //     <li key={item.id}>
    //       <a href={item.url}>{item.label}</a>
    //     </li>
    //   ))}
    // </ul>
    <footer>Footer</footer>
  );
};

export default Footer;
